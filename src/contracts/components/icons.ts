export interface ArrowIconProps {
  direction?: ArrowIconDirection;
}

export enum ArrowIconDirection {
  up = "up",
  right = "right",
  down = "down",
  left = "left",
}

export declare namespace ChevronIcon {}
export interface ChevronIconProps {
  direction?: ChevronIconDirection;
}

export enum ChevronIconDirection {
  up = "up",
  right = "right",
  down = "down",
  left = "left",
}
